# v3.1.2 Coupon Typecheck Hotfix

Fixes duplicate `MarketplaceCoupon` exports by renaming the legacy platform-operations coupon model to `MarketplaceOperationsCoupon`.

Also converts `FormEvent` to a type-only import under `verbatimModuleSyntax`.
