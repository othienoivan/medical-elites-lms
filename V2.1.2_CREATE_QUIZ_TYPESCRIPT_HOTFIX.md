# Medical Elites LMS v2.1.2

## Create Quiz TypeScript hotfix

- Replaced the invalid `questionBank` reference with the existing typed `questions` collection returned by `useQuestions()`.
- This restores TypeScript inference for both the `.find()` callback and option mapping.
- Updated both application source trees.

Deploy after running `npm install`, `npm run build`, and `firebase deploy --only firestore:rules,hosting`.
