import type { Wallet } from "../../domains/finance";
const money = (amount = 0, currency = "UGX") => new Intl.NumberFormat("en-UG", { style: "currency", currency, maximumFractionDigits: 0 }).format(amount);
export default function WalletSummaryCard({ wallet }: { wallet: Wallet }) { return <article className="rounded-2xl bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-wide text-slate-500">{wallet.ownerType} wallet</p><p className="mt-1 text-sm text-slate-600">{wallet.currency} · {wallet.status}</p></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">{wallet.id.slice(0,8)}</span></div><p className="mt-5 text-3xl font-black text-slate-950">{money(wallet.availableBalance, wallet.currency)}</p><div className="mt-4 grid grid-cols-2 gap-3 text-sm"><div><p className="text-slate-500">Pending</p><p className="font-bold">{money(wallet.pendingBalance, wallet.currency)}</p></div><div><p className="text-slate-500">Frozen</p><p className="font-bold">{money(wallet.frozenBalance, wallet.currency)}</p></div></div></article> }
