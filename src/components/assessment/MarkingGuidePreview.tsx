import Card from "../ui/Card";
import useQuestions from "../../hooks/useQuestions";
import type { Examination } from "../../models/Examination";
export default function MarkingGuidePreview({examination}:{examination:Examination}){const {questions}=useQuestions();let n=1;return <Card><h2 className="text-xl font-bold text-slate-950">Marking Guide Preview</h2><div className="mt-5 space-y-5">{examination.sections.map(section=><section key={section.id}><h3 className="font-bold uppercase">{section.title}</h3>{section.questions.map(ref=>{const q=questions.find(x=>x.id===ref.questionId);return <div key={ref.id} className="mt-3 rounded-xl border p-4"><p className="font-semibold">{n++}. {q?.questionText||"Question not found"} <span className="text-slate-500">({ref.marks} marks)</span></p><p className="mt-2 text-sm"><strong>Answer:</strong> {q?.correctAnswer||"Not provided"}</p>{q?.explanation&&<p className="mt-1 text-sm text-slate-600"><strong>Rationale:</strong> {q.explanation}</p>}</div>})}</section>)}</div></Card>}
