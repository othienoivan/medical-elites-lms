import { Link, useParams } from "react-router-dom";
import useAuth from "../../hooks/useAuth";
import { KnowledgeArticleCard, KnowledgeService, knowledgeCategories } from "../../domains/knowledge";
export default function KnowledgeCategoryPage(){const {category=""}=useParams();const {role}=useAuth();const meta=knowledgeCategories.find(item=>item.id===category);const articles=KnowledgeService.listByCategory(category,role);return <main className="mx-auto min-h-screen max-w-6xl px-4 py-10 sm:px-6"><Link to="/help" className="text-sm font-bold text-blue-700">← Knowledge Center</Link><h1 className="mt-4 text-3xl font-black">{meta?.title??category.replaceAll("-"," ")}</h1><p className="mt-2 text-slate-600">{meta?.description}</p><section className="mt-8 grid gap-4 md:grid-cols-2">{articles.map(article=><KnowledgeArticleCard key={article.id} article={article}/>)}</section>{articles.length===0&&<div className="mt-8 rounded-2xl border border-dashed p-10 text-center text-slate-600">No published article is available in this category for your role.</div>}</main>}
