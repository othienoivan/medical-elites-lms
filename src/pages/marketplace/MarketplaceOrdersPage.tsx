import { PackageCheck } from "lucide-react";
import { useEffect, useState } from "react";
import useAuth from "../../hooks/useAuth";
import { MarketplaceCommerceService, type MarketplaceOrder } from "../../domains/marketplace";
function money(amount:number,currency:string){return new Intl.NumberFormat("en-UG",{style:"currency",currency,maximumFractionDigits:0}).format(amount)}
export default function MarketplaceOrdersPage(){const {currentUser}=useAuth();const [orders,setOrders]=useState<MarketplaceOrder[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{if(!currentUser)return;MarketplaceCommerceService.listOrders(currentUser.uid).then(setOrders).finally(()=>setLoading(false))},[currentUser]);if(loading)return <div className="p-12 text-center">Loading orders…</div>;return <div className="min-h-screen bg-slate-50"><main className="mx-auto max-w-5xl px-4 py-10"><div className="flex items-center gap-3"><PackageCheck className="text-cyan-700"/><h1 className="text-3xl font-black">My marketplace orders</h1></div><div className="mt-8 space-y-4">{orders.length===0?<div className="rounded-2xl border bg-white p-10 text-center">No marketplace orders yet.</div>:orders.map(order=><article key={order.id} className="rounded-2xl border bg-white p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-black">{order.title}</h2><p className="mt-1 text-sm text-slate-600">{order.itemCount||order.productIds?.length||1} item(s) · {order.transactionReference}</p></div><div className="text-right"><p className="font-black">{money(order.amount.amount,order.amount.currency)}</p><span className="mt-1 inline-block rounded-full bg-slate-100 px-3 py-1 text-xs font-bold uppercase">{order.status}</span></div></div></article>)}</div></main></div>}
