import { useCallback, useEffect, useState } from "react";
import { collection, getDocs, orderBy, query, updateDoc, doc, serverTimestamp } from "firebase/firestore";
import { db } from "../../config/firebase";
import PlatformLayout from "../../components/platform/PlatformLayout";
import type { MarketplaceProduct } from "../../domains/marketplace";
export default function PlatformMarketplacePage(){const [items,setItems]=useState<MarketplaceProduct[]>([]);const [loading,setLoading]=useState(true);const load=useCallback(async()=>{setLoading(true);const snap=await getDocs(query(collection(db,"marketplaceProducts"),orderBy("updatedAt","desc")));setItems(snap.docs.map(s=>({id:s.id,...s.data()}) as MarketplaceProduct));setLoading(false)},[]);useEffect(()=>{void load()},[load]);async function setStatus(id:string,status:MarketplaceProduct["status"]){await updateDoc(doc(db,"marketplaceProducts",id),{status,updatedAt:serverTimestamp(),...(status==="published"?{publishedAt:serverTimestamp()}: {})});await load()}
return <PlatformLayout title="Marketplace Operations" subtitle="Review, publish, hide and archive products without affecting the academic LMS."><div className="rounded-2xl border bg-white"><div className="border-b p-5"><h2 className="text-xl font-black">Product moderation queue</h2></div>{loading?<div className="p-8 text-center">Loading products…</div>:items.length===0?<div className="p-8 text-center text-slate-600">No marketplace products found.</div>:<div className="divide-y">{items.map(item=><div key={item.id} className="flex flex-col gap-4 p-5 lg:flex-row lg:items-center lg:justify-between"><div><div className="flex flex-wrap items-center gap-2"><h3 className="font-black">{item.title}</h3><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-bold uppercase">{item.status}</span></div><p className="mt-1 text-sm text-slate-600">{item.sellerName} · {item.type.replaceAll("_"," ")} · {item.price.currency} {item.price.amount.toLocaleString()}</p></div><div className="flex flex-wrap gap-2"><button onClick={()=>void setStatus(item.id,"published")} className="rounded-lg bg-emerald-600 px-3 py-2 text-sm font-bold text-white">Publish</button><button onClick={()=>void setStatus(item.id,"review")} className="rounded-lg border px-3 py-2 text-sm font-bold">Review</button><button onClick={()=>void setStatus(item.id,"hidden")} className="rounded-lg border px-3 py-2 text-sm font-bold">Hide</button><button onClick={()=>void setStatus(item.id,"archived")} className="rounded-lg border px-3 py-2 text-sm font-bold text-red-700">Archive</button></div></div>)}</div>}</div></PlatformLayout>}
