import { useEffect, useState } from "react"; import PlatformLayout from "../../../components/platform/PlatformLayout"; import { FinanceService } from "../../../domains/finance";
export default function Page(){const [records,setRecords]=useState<Array<{id:string}>>([]);const [error,setError]=useState("");useEffect(()=>{void FinanceService.list("withdrawals").then(setRecords).catch((e:unknown)=>setError(e instanceof Error?e.message:"Unable to load records."))},[]);return <PlatformLayout title="Withdrawals" subtitle="Server-controlled finance records. Browser clients have read-only access.">{error&&<p className="rounded-xl bg-red-50 p-4 text-red-700">{error}</p>}<div className="rounded-2xl bg-white p-6 shadow-sm"><p className="text-sm text-slate-500">Total records</p><p className="mt-2 text-4xl font-black">{records.length}</p>{records.length===0&&<p className="mt-4 text-slate-500">No records yet.</p>}</div></PlatformLayout>}
