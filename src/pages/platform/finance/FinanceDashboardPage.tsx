import { useEffect, useState } from "react"; import { Banknote, CreditCard, FileText, WalletCards } from "lucide-react"; import PlatformLayout from "../../../components/platform/PlatformLayout"; import { FinanceService } from "../../../domains/finance";
export default function FinanceDashboardPage(){const [counts,setCounts]=useState({subscriptions:0,wallets:0,invoices:0,payments:0});useEffect(()=>{void Promise.all([FinanceService.list("subscriptions"),FinanceService.list("wallets"),FinanceService.list("invoices"),FinanceService.list("payments")]).then(([a,b,c,d])=>setCounts({subscriptions:a.length,wallets:b.length,invoices:c.length,payments:d.length})).catch(()=>undefined)},[]);const cards=[["Subscriptions",counts.subscriptions,CreditCard],["Wallets",counts.wallets,WalletCards],["Invoices",counts.invoices,FileText],["Payments",counts.payments,Banknote]] as const;return <PlatformLayout title="Finance Centre" subtitle="Commerce, billing, immutable ledgers and revenue operations."><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value,Icon])=><div key={label} className="rounded-2xl bg-white p-6 shadow-sm"><Icon className="text-cyan-700"/><p className="mt-5 text-sm font-semibold text-slate-500">{label}</p><p className="mt-1 text-3xl font-black">{value}</p></div>)}</div><div className="mt-6 rounded-2xl bg-white p-6 shadow-sm"><h2 className="text-xl font-black">Finance architecture status</h2><p className="mt-2 text-slate-600">Double-entry journal validation, CQRS-ready repositories, domain events and compensating workflows are active foundations. Financial writes remain server-controlled.</p></div></PlatformLayout>}
