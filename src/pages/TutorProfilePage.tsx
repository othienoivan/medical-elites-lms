import { useEffect,useState } from "react"; import { Save,UserRound } from "lucide-react"; import TutorLayout from "../components/layout/TutorLayout"; import Button from "../components/ui/Button"; import Card from "../components/ui/Card"; import Input from "../components/ui/Input"; import useAuth from "../hooks/useAuth"; import { updateOwnTutorProfile } from "../firebase/tutorProfile";
export default function TutorProfilePage(){const{currentUser,userProfile}=useAuth();const profile=userProfile as Record<string,unknown>|null;const[form,setForm]=useState({fullName:"",phoneNumber:"",professionalHeadline:"",bio:"",institutionName:"",specialties:"",qualifications:"",profilePhoto:""});const[saving,setSaving]=useState(false);const[message,setMessage]=useState("");useEffect(()=>{setForm({fullName:String(profile?.fullName??""),phoneNumber:String(profile?.phoneNumber??profile?.phone??""),professionalHeadline:String(profile?.professionalHeadline??""),bio:String(profile?.bio??""),institutionName:String(profile?.institutionName??""),specialties:Array.isArray(profile?.specialties)?(profile?.specialties as string[]).join(", "):"",qualifications:Array.isArray(profile?.qualifications)?(profile?.qualifications as string[]).join(", "):"",profilePhoto:String(profile?.profilePhoto??"")});},[userProfile]);const set=(key:keyof typeof form)=>(event:React.ChangeEvent<HTMLInputElement|HTMLTextAreaElement>)=>setForm(current=>({...current,[key]:event.target.value}));async function save(event:React.FormEvent){event.preventDefault();if(!currentUser)return;setSaving(true);setMessage("");try{await updateOwnTutorProfile({fullName:form.fullName.trim(),phoneNumber:form.phoneNumber.trim(),professionalHeadline:form.professionalHeadline.trim(),bio:form.bio.trim(),institutionName:form.institutionName.trim(),profilePhoto:form.profilePhoto.trim(),specialties:form.specialties.split(",").map(v=>v.trim()).filter(Boolean),qualifications:form.qualifications.split(",").map(v=>v.trim()).filter(Boolean)});setMessage("Tutor profile updated successfully.");}catch(error){console.error(error);setMessage("Tutor profile could not be updated. Please try again.");}finally{setSaving(false)}}return <TutorLayout title="My Tutor Profile" subtitle="Manage the professional details shown to learners and marketplace visitors."><div className="mx-auto max-w-5xl space-y-6"><section className="rounded-3xl bg-gradient-to-r from-emerald-700 to-cyan-700 p-6 text-white sm:p-8"><UserRound size={40}/><h2 className="mt-3 text-2xl font-black sm:text-3xl">Professional Profile</h2><p className="mt-2 text-emerald-50">Keep your tutor identity, qualifications and teaching profile accurate.</p></section><Card><form onSubmit={save} className="space-y-5"><div className="grid gap-5 md:grid-cols-2"><label className="font-semibold">Full name<Input value={form.fullName} onChange={set("fullName")} required/></label><label className="font-semibold">Email<Input value={currentUser?.email??""} disabled/></label><label className="font-semibold">Telephone<Input value={form.phoneNumber} onChange={set("phoneNumber")}/></label><label className="font-semibold">Institution<Input value={form.institutionName} onChange={set("institutionName")}/></label><label className="font-semibold md:col-span-2">Professional headline<Input value={form.professionalHeadline} onChange={set("professionalHeadline")}/></label><label className="font-semibold md:col-span-2">Profile photo URL<Input value={form.profilePhoto} onChange={set("profilePhoto")}/></label><label className="font-semibold md:col-span-2">Specialties (comma separated)<Input value={form.specialties} onChange={set("specialties")}/></label><label className="font-semibold md:col-span-2">Qualifications (comma separated)<Input value={form.qualifications} onChange={set("qualifications")}/></label></div><label className="block font-semibold">Professional biography<textarea value={form.bio} onChange={set("bio")} rows={6} className="mt-2 w-full rounded-xl border border-slate-300 bg-white px-4 py-3 text-slate-900 outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-100"/></label>{message&&<p className="rounded-xl bg-blue-50 p-3 text-sm font-semibold text-blue-800">{message}</p>}<Button type="submit" disabled={saving}><Save size={18}/>{saving?"Saving...":"Save Tutor Profile"}</Button></form></Card></div></TutorLayout>}
