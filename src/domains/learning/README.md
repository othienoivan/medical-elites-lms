# Learning bounded context

The current production learning module remains in the legacy folders during RC3. This bounded context is the target home for course units, modules, lessons, enrolments and progress after one workflow at a time has repository/service coverage and regression tests.
