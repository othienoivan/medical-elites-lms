import { BookOpenCheck, ExternalLink, X } from "lucide-react";
import { Link } from "react-router-dom";
import type { KnowledgeArticle } from "../domain/models";

export default function ContextualHelpPanel({ open, onClose, articles, pathname }: { open: boolean; onClose: () => void; articles: KnowledgeArticle[]; pathname: string }) {
  if (!open) return null;
  return <div className="fixed inset-0 z-[80] flex justify-end bg-slate-950/30" role="dialog" aria-modal="true" aria-label="Contextual help"><aside className="h-full w-full max-w-md overflow-y-auto bg-white p-6 shadow-2xl"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2 text-blue-700"><BookOpenCheck size={22} /><span className="font-black">Explain This Page</span></div><p className="mt-2 break-all text-xs text-slate-500">{pathname}</p></div><button type="button" onClick={onClose} className="rounded-xl border p-2 text-slate-600 hover:bg-slate-50" aria-label="Close help"><X size={20} /></button></div><div className="mt-7 space-y-3">{articles.length ? articles.map((article) => <Link key={article.id} to={`/help/articles/${article.slug}`} onClick={onClose} className="block rounded-2xl border p-4 hover:border-blue-300 hover:bg-blue-50"><p className="font-black text-slate-900">{article.title}</p><p className="mt-1 text-sm text-slate-600">{article.summary}</p></Link>) : <div className="rounded-2xl border border-dashed p-5 text-sm text-slate-600">No page-specific article is available yet. Search the full Knowledge Center for related guidance.</div>}</div><Link to={`/help?context=${encodeURIComponent(pathname)}`} onClick={onClose} className="mt-6 inline-flex items-center gap-2 rounded-xl bg-blue-700 px-4 py-3 font-bold text-white hover:bg-blue-800">Open Knowledge Center <ExternalLink size={16} /></Link></aside></div>;
}
