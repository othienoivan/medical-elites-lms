# Storage Rules Regression Patch

This patch preserves the explicit upload and profile match declarations expected by the regression suite while adding the recursive tenant and user namespace controls required by the platform baseline validator.

Files:
- `storage.rules`
- `src/firebase/storage.rules`

The two rules files are identical.
