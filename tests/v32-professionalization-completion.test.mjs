import test from "node:test"; import assert from "node:assert/strict"; import { readFile } from "node:fs/promises";
const read=(path)=>readFile(path,"utf8");
test("global search is routed and available from header actions",async()=>{const [router,header]=await Promise.all([read("src/routes/AppRouter.tsx"),read("src/components/HeaderActions.tsx")]);assert.match(router,/path="\/search"/);assert.match(header,/Open global search/);});
test("student professional navigation includes commerce and mobile access",async()=>{const source=await read("src/components/layout/StudentLayout.tsx");for(const route of ["/student/marketplace","/student/purchases","/student/library","/student/wishlist"])assert.match(source,new RegExp(route.replaceAll("/","\\/")));assert.match(source,/Student mobile navigation/);});
test("commerce asset compatibility foundation exists",async()=>{const [model,service]=await Promise.all([read("src/domains/marketplace/domain/commerceAsset.ts"),read("src/domains/marketplace/application/commerce-asset-service.ts")]);assert.match(model,/interface CommerceAsset/);assert.match(model,/marketplaceProductToCommerceAsset/);assert.match(service,/validateBundleOwnership/);});
